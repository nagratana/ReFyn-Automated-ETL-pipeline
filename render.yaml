services:
  - type: web
    name: refyn
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir flask_app app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
